* Object.new with #clone
* include
* extend
* include self?
* singleton methods
* mixing modules into classes
* mixing module into objects
* Ancestors (define a method then include a module that also defines it. Which one wins?)
*